# Pinned to the versions actually used to produce the headline numbers (Python 3.11).
# Kaggle's own environment runs these preinstalled; pin here is for an exact local repro.
pandas==2.3.0
numpy==2.3.0
matplotlib==3.10.3
lightgbm==4.6.0
shap==0.51.0
scipy==1.16.0
scikit-learn==1.7.1
ipython==9.3.0
